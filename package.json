{
  "name": "platform-admin-console",
  "version": "1.0.0",
  "private": true,
  "description": "Private admin + seller dashboard (React + TypeScript + Vite). Talks to the backend REST API with HttpOnly session cookies — never deployed to GitHub Pages, never bundles the GitHub token.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
